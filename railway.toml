[build]
builder = "nixpacks"
buildCommand = "npx prisma generate && npm run build && cp -r src/generated dist/generated"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
